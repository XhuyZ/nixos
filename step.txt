nmtui

ping google.com
nix-shell -p git
git clone https://github.com/XhuyZ/nixos
sudo nix --experimental-features "nix-command flakes" run github:nix-community/disko/latest -- --mode destroy,format,mount ~/nixos/hosts/xxx/disk-config.nix
cd ~
cd nixos
sudo nixos-install --flake .#machine

STEP 4 — Storage RAID (IMPORTANT)

👉 làm sau khi system đã boot

sudo wipefs -a /dev/sd[a-d]

sudo mkfs.btrfs -f -d raid1 -m raid1 \
  /dev/sda /dev/sdb /dev/sdc /dev/sdd

	sudo mkdir -p /mnt/storage

	sudo mount /dev/sda /mnt/storage
	df -h /mnt/storage
	osudo btrfs filesystem show


	systemd-tty-ask-password-agent
	cryptsetup-askpass
sudo ip link set enp1s0 up
sudo ip addr add 192.168.1.50/24 dev enp1s0
sudo ip route add default via 192.168.1.1
