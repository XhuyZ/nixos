{
  age = {
    secrets = {
      # traefik = {
      #   file = ../../secrets/traefik.age;
      #   owner = "traefik";
      # };
      xhuyz-secrets = {
        file = ../../secrets/xhuyz-secrets.age;
        owner = "xhuyz";
      };
    };
  };
}
